# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

## Данные и модели данных

### Интерфейсы данных

В проекте есть два основных типа данных, которые мы будем использовать:

Товар (IProduct) - то, что продается в магазине

interface IProduct {
  id: string;        // айди товара, уникальный
  title: string;     // название
  image: string;     // картинка
  category: string;  // категория (софт-скил, хард-скил и тд)
  price: number | null; // цена, null значит "бесценный" (не продается)
  description: string; // описание
}


Покупатель (IBuyer) - кто покупает

type TPayment = 'card' | 'cash' | ''; // способ оплаты

interface IBuyer {
  payment: TPayment;  // оплата картой/налом
  address: string;    // адрес доставки
  email: string;      // почта
  phone: string;      // телефон
}
Модели данных
У нас будет три класса, каждый отвечает за свою часть данных.

### 1. Класс CatalogModel (Модель каталога товаров)

**Назначение:**  
Управляет данными каталога товаров. Отвечает за хранение массива всех доступных товаров, а также за выбор конкретного товара для просмотра в модальном окне.

**Конструктор:**  
`constructor()` — не принимает параметров, инициализирует пустой каталог.

**Поля класса:**
- `_products: IProduct[]` — приватное поле, хранит массив всех товаров
- `_selectedCard: IProduct | null` — приватное поле, хранит выбранный для просмотра товар

**Методы:**
- `setProducts(products: IProduct[]): void` — сохраняет массив товаров в модель
- `getProducts(): IProduct[]` — возвращает весь массив товаров
- `getProduct(id: string): IProduct | undefined` — возвращает товар по его идентификатору
- `setSelectedCard(product: IProduct): void` — сохраняет выбранный товар
- `getSelectedCard(): IProduct | null` — возвращает выбранный товар

---

### 2. Класс CartModel (Модель корзины)

**Назначение:**  
Управляет товарами, добавленными пользователем в корзину. Обеспечивает подсчет количества товаров, общей стоимости и проверку наличия.

**Конструктор:**  
`constructor()` — не принимает параметров, инициализирует пустую корзину.

**Поля класса:**
- `_items: IProduct[]` — приватное поле, хранит массив товаров в корзине

**Методы:**
- `getItems(): IProduct[]` — возвращает все товары в корзине
- `addItem(product: IProduct): void` — добавляет товар в корзину (не добавляет бесценные товары)
- `removeItem(productId: string): void` — удаляет товар из корзины по id
- `clearCart(): void` — полностью очищает корзину
- `getTotalPrice(): number` — возвращает общую стоимость товаров
- `getItemCount(): number` — возвращает количество товаров в корзине
- `hasItem(productId: string): boolean` — проверяет наличие товара в корзине

---

### 3. Класс BuyerModel (Модель покупателя)

**Назначение:**  
Управляет персональными данными покупателя для оформления заказа. Обеспечивает частичное обновление полей и валидацию данных.

**Конструктор:**  
`constructor()` — не принимает параметров, инициализирует все поля пустыми строками.

**Поля класса:**
- `_payment: TPayment` — выбранный способ оплаты
- `_address: string` — адрес доставки
- `_email: string` — email покупателя
- `_phone: string` — телефон покупателя

**Методы:**
- `setData(data: Partial<IBuyer>): void` — частичное обновление данных
- `getData(): IBuyer` — возвращает все данные покупателя
- `clearData(): void` — очищает все поля
- `validate(): TBuyerErrors` — проверяет заполненность полей, возвращает объект с ошибками


## Слой коммуникации

### Класс LarekApi (Слой коммуникации)

**Назначение:**  
Отвечает за взаимодействие с сервером. Использует базовый класс Api для отправки запросов.

**Конструктор:**  
`constructor(api: IApi)` — принимает экземпляр Api для выполнения запросов

**Поля класса:**
- `_api: IApi` — приватное поле, хранит экземпляр Api

**Методы:**
- `getProductList(): Promise<IProduct[]>` — выполняет GET-запрос к эндпоинту `/product/` и возвращает массив товаров
- `sendOrder(order: IOrder): Promise<IOrderResult>` — выполняет POST-запрос к эндпоинту `/order/` для отправки заказа

## Слой представления (View)

Классы представления отвечают за отображение данных на странице и взаимодействие с пользователем. Каждый класс инкапсулирует определённый блок разметки, наследуется от базового класса `Component` и использует брокер событий `EventEmitter` для оповещения презентера о действиях пользователя.

---

### Класс Card<T>

Базовый класс для всех видов карточек товара. Содержит общие элементы и логику установки данных. Параметр типа `T` позволяет передавать различные данные для конкретных вариантов карточек.

**Конструктор:**  
`constructor(container: HTMLElement, actions?: ICardActions)`  
- `container` — корневой DOM‑элемент карточки  
- `actions` — опциональный объект с колбэками (например, `onClick`)

**Поля класса:**  
- `_title: HTMLElement` — элемент для названия товара  
- `_price: HTMLElement` — элемент для отображения цены  
- `_image?: HTMLImageElement` — элемент изображения (присутствует не во всех карточках)  
- `_category?: HTMLElement` — элемент категории (опционально)  
- `_button?: HTMLButtonElement` — кнопка действия (опционально)

**Методы класса:**  
- `set title(value: string): void` — устанавливает текст названия  
- `set price(value: number | null): void` — устанавливает цену; если `null`, выводит «Бесценно»  
- `set image(value: string): void` — устанавливает изображение (через `setImage`)  
- `set category(value: string): void` — устанавливает категорию и соответствующий CSS‑класс для фона  
- `set buttonText(value: string): void` — меняет текст на кнопке  
- `set disabled(value: boolean): void` — блокирует или разблокирует кнопку

---

### Класс CatalogCard (наследует Card<IProduct>)

Карточка товара в каталоге (галерее) на главной странице.

**Конструктор:**  
`constructor(container: HTMLElement, actions?: ICardActions)` — вызывает родительский конструктор.

**Поля класса:**  
Наследует все поля от `Card`.

**Методы класса:**  
Наследует все методы от `Card`.

---

### Класс BasketCard (наследует Card<IProduct>)

Карточка товара в корзине. Содержит название, цену, порядковый номер и кнопку удаления.

**Конструктор:**  
`constructor(container: HTMLElement, actions?: ICardActions)` — вызывает родительский конструктор.

**Поля класса:**  
- `_index: HTMLElement` — элемент для отображения номера товара в списке (дополнительно к унаследованным)

**Методы класса:**  
- `set index(value: number): void` — устанавливает порядковый номер

---

### Класс PreviewCard (наследует Card<IProduct>)

Детальная карточка товара, отображаемая в модальном окне. Добавляет описание и управляет текстом кнопки в зависимости от наличия товара в корзине.

**Конструктор:**  
`constructor(container: HTMLElement, actions?: ICardActions)` — вызывает родительский конструктор.

**Поля класса:**  
- `_description: HTMLElement` — элемент для описания товара (дополнительно к унаследованным)

**Методы класса:**  
- `set description(value: string): void` — устанавливает текст описания  
- `set buttonState(inBasket: boolean): void` — изменяет текст кнопки: «В корзину» или «Удалить из корзины»

---

### Класс Form<T>

Базовый класс для всех форм приложения. Управляет полями ввода, валидацией, кнопкой отправки и генерирует события при изменениях.

**Конструктор:**  
`constructor(container: HTMLFormElement, protected events: EventEmitter)`  
- `container` — корневой элемент формы (`<form>`)  
- `events` — экземпляр брокера событий для генерации событий

**Поля класса:**  
- `_submit: HTMLButtonElement` — кнопка отправки формы  
- `_errors: HTMLElement` — элемент для вывода сообщений об ошибках  
- `inputs: Map<string, HTMLInputElement>` — коллекция полей ввода (ключ — имя поля)

**Методы класса:**  
- `set valid(value: boolean): void` — управляет доступностью кнопки отправки  
- `set errors(value: string): void` — устанавливает текст ошибки  
- `render(data: Partial<T> & IFormState): HTMLElement` — обновляет поля формы на основе переданных данных и состояния валидности  
- `protected onInputChange(field: keyof T, value: string): void` — вызывается при изменении поля, генерирует событие `form:change` с именем поля и значением

---

### Класс OrderForm (наследует Form<IOrder>)

Форма первого шага оформления заказа: выбор способа оплаты и ввод адреса.

**Конструктор:**  
`constructor(container: HTMLFormElement, events: EventEmitter)` — вызывает родительский конструктор.

**Поля класса:**  
Наследует все поля от `Form`. Дополнительно содержит переключатели способов оплаты (карта/наличные) — они находятся внутри `container` и обрабатываются через `onInputChange`.

**Методы класса:**  
Наследует все методы от `Form`.

---

### Класс ContactsForm (наследует Form<IBuyer>)

Форма второго шага: ввод email и телефона.

**Конструктор:**  
`constructor(container: HTMLFormElement, events: EventEmitter)` — вызывает родительский конструктор.

**Поля класса:**  
Наследует все поля от `Form`.

**Методы класса:**  
Наследует все методы от `Form`.

---

### Класс Modal

Управляет модальным окном: открытие, закрытие, установка содержимого. Не имеет наследников.

**Конструктор:**  
`constructor(container: HTMLElement, protected events: EventEmitter)`  
- `container` — корневой элемент модального окна  
- `events` — брокер событий

**Поля класса:**  
- `_content: HTMLElement` — контейнер для контента  
- `_closeButton: HTMLButtonElement` — кнопка закрытия

**Методы класса:**  
- `set content(value: HTMLElement): void` — помещает переданный элемент в контейнер контента  
- `open(): void` — открывает модальное окно  
- `close(): void` — закрывает модальное окно  
- `render(data: IModalData): HTMLElement` — обновляет контент и возвращает корневой элемент

---

### Класс Basket

Отображает список товаров в корзине, общую сумму и кнопку оформления.

**Конструктор:**  
`constructor(container: HTMLElement, protected events: EventEmitter)`  
- `container` — корневой элемент корзины  
- `events` — брокер событий

**Поля класса:**  
- `_list: HTMLElement` — контейнер для карточек товаров  
- `_total: HTMLElement` — элемент для отображения общей суммы  
- `_button: HTMLButtonElement` — кнопка «Оформить»

**Методы класса:**  
- `set items(items: HTMLElement[]): void` — заменяет содержимое списка переданными элементами  
- `set total(value: number): void` — устанавливает текст общей суммы  
- `set disabled(value: boolean): void` — блокирует кнопку, если корзина пуста  
- `render(data: IBasketView): HTMLElement` — обновляет отображение корзины

---

### Класс Page

Управляет глобальными элементами страницы: галереей товаров, счётчиком корзины, блокировкой прокрутки.

**Конструктор:**  
`constructor(container: HTMLElement, protected events: EventEmitter)`  
- `container` — корневой элемент страницы (обычно `<body>` или обёртка)  
- `events` — брокер событий

**Поля класса:**  
- `_catalog: HTMLElement` — контейнер для галереи карточек  
- `_basketCounter: HTMLElement` — элемент счётчика товаров в корзине  
- `_wrapper: HTMLElement` — обёртка страницы (для блокировки скролла)

**Методы класса:**  
- `set catalog(items: HTMLElement[]): void` — заполняет галерею карточками  
- `set counter(value: number): void` — обновляет счётчик корзины  
- `set locked(value: boolean): void` — блокирует/разблокирует прокрутку страницы  
- `render(data: IPageView): HTMLElement` — обновляет состояние страницы

---

### Класс Success

Отображает сообщение об успешном оформлении заказа с итоговой суммой и кнопкой закрытия.

**Конструктор:**  
`constructor(container: HTMLElement, actions?: ISuccessActions)`  
- `container` — корневой элемент  
- `actions` — опциональный объект с колбэками (например, `onClick`)

**Поля класса:**  
- `_close: HTMLButtonElement` — кнопка закрытия  
- `_total: HTMLElement` — элемент для отображения суммы

**Методы класса:**  
- `set total(value: number): void` — устанавливает текст с суммой списания


## События приложения
Все события генерируются через брокер `EventEmitter`. В презентере на них подписываются соответствующие обработчики.

### События карточек
- `card:select` — клик по карточке в каталоге. Данные: `{ id: string }`
- `card:toBasket` — добавление товара в корзину (из превью). Данные: `{ id: string }`
- `card:fromBasket` — удаление товара из корзины (из превью). Данные: `{ id: string }`

### События корзины
- `basket:remove` — удаление товара из корзины (по кнопке в карточке корзины). Данные: `{ id: string }`
- `basket:order` — переход к оформлению заказа (клик по кнопке в корзине). Данные: отсутствуют

### События форм
- `form:change` — изменение любого поля в форме. Данные: `{ field: string, value: string }`
- `order:paymentChange` — изменение способа оплаты в форме заказа. Данные: `{ payment: 'card' | 'cash' }`
- `order:submit` — отправка формы заказа (первый шаг)
- `contacts:submit` — отправка формы контактов (второй шаг)

### События модального окна
- `modal:open` — открытие модального окна
- `modal:close` — закрытие модального окна

### Прочие события
- `success:close` — закрытие окна успешного заказа

### События моделей данных
- `catalog:changed` — изменение списка товаров в каталоге. Данные: `{ products: IProduct[] }`
- `card:selected` — выбор товара для просмотра. Данные: `{ product: IProduct | null }`
- `cart:changed` — изменение состава корзины. Данные: `{ items: IProduct[] }`
- `buyer:changed` — изменение данных покупателя. Данные: `IBuyer`

## Презентер
Презентер реализован в файле `src/main.ts` и отвечает за связь моделей и представлений. Он подписывается на события, генерируемые моделями и компонентами интерфейса, и выполняет соответствующие действия: обновление отображения, изменение данных, открытие модальных окон и т.д.

Основные задачи презентера:
- Загрузка каталога товаров с сервера при инициализации.
- Отрисовка галереи товаров на главной странице.
- Открытие карточки товара в модальном окне при клике.
- Добавление/удаление товаров из корзины.
- Управление состоянием форм оформления заказа.
- Отправка заказа на сервер и отображение результата.

Презентер не содержит логики изменения данных — он только вызывает методы моделей и обновляет представления в ответ на события. Генерация событий происходит только в моделях и представлениях.